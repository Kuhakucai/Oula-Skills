interface:
  display_name: "Grill Me"
  short_description: "Sharpen a plan through interview"
  default_prompt: "Use $grill-me to challenge my plan one decision at a time until we reach a shared understanding."
policy:
  allow_implicit_invocation: false
